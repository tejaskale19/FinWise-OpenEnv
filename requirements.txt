# FinWise OpenEnv — Python Dependencies
# Designed to run on vcpu=2, memory=8GB (hackathon infra constraint)

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation (OpenEnv typed models)
pydantic==2.7.1

# LLM client (mandatory — must use OpenAI client per hackathon rules)
openai>=2.7.2

# HTTP
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.1

# OpenEnv runtime/validator compatibility
openenv-core>=0.2.0
